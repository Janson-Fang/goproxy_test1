bin/
goproxy
goproxy.exe
*.exe
*.log
data/
__pycache__/
*.pyc
# 各种「写点东西给命令读」的临时文件：发布说明、提交信息……
# 它们只是工具链的中间产物，但用 `git add -A` 时会一起被带进去（误提交过两次）。
# 用后缀规则一次挡住，别再逐个补文件名 —— 补一个漏一个。
.*.tmp

# 配置数据库（v0.9.0 起配置的真源）。本地跑起来就会生成，
# 里面带着 admin_token 与密码哈希，绝对不要提交。
goproxy.db
goproxy.db-wal
goproxy.db-shm
