fix(install): 按二进制能力分流，别再硬套 SQLite 流程

install-smoke 在 main 上红了：矩阵里那一半固定版本的用例装的是 v0.3.0，
而新版 install.sh 会调 `goproxy -c ... -config-import` —— 那个二进制根本没有
这个开关，报的是「flag provided but not defined」，现象是「装不上」，
真正的原因却是版本不匹配。

脚本是从 main 取的（jsdelivr），二进制却来自 Release，两者天然可能不同步；
再加上 README 明确把 `VERSION=` 固定版本列为推荐做法，所以「脚本要按装出来的
那个二进制的能力干活」是必须的，不是可选的。

改动：
  · install.sh 装完二进制后探一次 `-h` 的自述有没有 -config-import，据此把
    CONFIG_BACKEND 定为 sqlite / json，再分流：
      sqlite → 现有流程（导入库、ExecStart 指库、提示里说「配置库」）
      json   → 原有流程（config.json 就是配置、提示语与 v0.8.0 逐字一致）
  · 新增 $SERVICE_CONFIG（单元与提示里 -c 的那个路径），两条路共用一个变量，
    避免写死 $CONFIG_DB 导致旧版服务指向一个不存在的库
  · 探测刻意不走管道：脚本是 set -o pipefail，而 `-h` 的退出码是 2，
    走管道会让整条判成失败、永远落进 json 分支（看起来「探测没生效」）

install-smoke.yml：
  · 「断言安装结果」「冒烟」两步改成按能力分支，不再写死 config.json /
    smoke.json —— 写死一种就会在另一半矩阵上失败，而报错指向「二进制起不来」
  · 幂等性一步补一条更重要的断言：重跑安装**不能拿过时的 config.json 覆盖库**
    （库才是真源，控制台里改过的路由只落在库里；被覆盖了导入还会报成功）
  · 新增 upgrade-sqlite job：v0.8.0（JSON 文件）→ 最新版（SQLite 库）跑完整
    升级路径，断言用户那条路由与 admin_token 都进了库、config.json 字节级未变、
    升级前的进程还活着、新版用库起来后路由照常服务。这是本次发版的关键路径，
    以前只有配置层的单测覆盖，装完之后串不串得起来没人验过

deploy_test.go：
  · TestInstallRendersWellFormedUnit 的变量替换表补 $SERVICE_CONFIG
  · 新增 TestInstallFallsBackForLegacyBinaries 守住三个不变量：探测确实存在
    且落在生效分支上、ExecStart 走 $SERVICE_CONFIG、两条路各有自己的收尾提示

README 补一段说明：历史版本照旧能装，靠的就是这个能力探测。
